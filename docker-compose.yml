version: "3.9"

services:
  automation:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - HEADLESS=true
    volumes:
      - .:/app
    working_dir: /app
    command: pytest -v --tb=short
    tty: true
    stdin_open: true
