version: "3.9"

services:
  automation:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    volumes:
      - ./reports:/app/reports
    working_dir: /app
    command: pytest tests/ui/chatbot/test_chatbot.py
    tty: true
    stdin_open: true
